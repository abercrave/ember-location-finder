import objectifyPath from '../../../utils/objectify-path';
import { module, test } from 'qunit';

module('Unit | Utility | objectify path');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = objectifyPath();
  assert.ok(result);
});
